/_build/
/deps/
/cover/
/doc/
erl_crash.dump
*.ez
*.beam
# Written by .claude/atp-mcp-launch.sh at MCP startup; contains the
# Isabelle server password — never commit.
/.isabelle_server_info

.claude-config

# StarExec / E prover build artifacts
/external/eprover-src/
/starexec/*.zip
/starexec/build/

# Host-installable static binaries (regenerate with scripts/make-host-binaries.sh)
/host-bin/
